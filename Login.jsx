//this is the component for the password//
const Login = ()=>{
    return <form>
        <label for="password">password</label> 
        <input type="password" placeholder="*****" id="password" name="password"></input>
    </form>;
};
export default Login;